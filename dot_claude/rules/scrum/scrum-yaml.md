---
paths: "scrum.yaml"
---

## Scrum Guidelines

* SSoT: Treat the `scrum.yaml` file as the single source of truth for scrum development
* Version control: Git commit every time making meaningful changes to `scrum.yaml` unless gitignore rules apply. The commit should involve only changes to `scrum.yaml`
