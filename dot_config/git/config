[user]
	email = 30277794+atusy@users.noreply.github.com
	name = atusy
[core]
	pager = delta
	quotepath = false
	editor = nvim
	hooksPath = ~/.config/git/hooks
[interactive]
	diffFilter = delta --color-only --features=interactive
[delta]
	features = line-numbers decorations
	whitespace-error-style = 22 reverse
	max-line-length = 0
[delta "decorations"]
	commit-decoration-style = bold yellow box ul
	file-style = bold yellow ul
	file-decoration-style = none
[color]
	ui = true
	diff = auto
	status = auto
	branch = auto
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
[alias]
	tracked = status -u no --short --branch
	graph-base = log --graph --date-order -C -M
	graph = graph-base --pretty=format:\"%h <%ad> [%an] %Cgreen%d%Creset %s\" --date=short
	gintonic-graph = graph-base --pretty=format:\"%h %Cgreen%d%Creset %s\"
	unstage = reset HEAD
	stage = add
	ff = merge --ff
	amend = commit --amend
	browse = "!deno run --allow-net --allow-run --allow-read https://deno.land/x/git_browse/bin/browse.ts"
[init]
	templatedir = ~/.config/git/template
	defaultBranch = main
[pull]
	ff = only
[push]
	useForceIfIncludes = true
[diff]
	algorithm = histogram
	colorMoved = dimmed-zebra
	colorMovedWS = allow-indentation-change
[difftool]
	prompt = false
[mergetool]
	prompt = false
[rebase]
	autoSquash = true
	rebaseMerges = true
[status]
	short = true
	branch = true
[tag]
	sort = v:refname
[advice]
	detachedHead = false
[fetch]
	prune = true
[credential]
	helper = cache
[commit]
	cleanup = scissors
	verbose = true
[credential "https://github.com"]
	helper = !/usr/bin/env gh auth git-credential
[credential "https://gist.github.com"]
	helper = !/usr/bin/env gh auth git-credential
